
public class Ex32 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
/*
 * Scanner 클래스
 	1. 형식 : Scanner stdin = new Scanner(System.in);
 		* Scanner 클래스의 생성자
 		- Scanner(String str);
 		- Scanner(File filename);
 		- Scanner(File filename, String charsetName);
 		- Scanner(InputStream source);
 		- Scanner(InputStream source, String charestName);
 		=> str : str은 읽어들일 문자열
 			ex)	String s="안녕하세요";
 				Scanner input = new Scanner(s);
 				
 		=> filename : File형의 객체
 		=> source : InputStream 형의 객체
 			ex)	Scanner input = new Scanner(System.in);
 			
 		=> charestName : 읽어들인 문자의 인코딩 방법
 	
 
 * Scanner 클래스의 주요 메소드
 		메소드										설명
 	============================================================================
 	boolean hasNext()					다음 토큰이 있으면 true를 반환
 	String next()						다음 토큰을 찾아 문자열로 반환
 	boolean nextBoolean()				다음 토큰을 이진 값으로 반환
 	byte nextByte()						다음 토큰을 바이트 값으로 반환
 	double nextDouble()					다음 토큰을 double 값으로 반환
 	float nextFloat()					다음 토큰을 float 값으로 반환
 	int nextInt()						다음 토큰을 Integer 값으로 반환
 	long nextLong()						다음 토큰을 long 값으로 반환
 	short nextShort()					다음 토큰을 short 값으로 반환
 	String nextLine()					하나의 라인을 문자열로 반환
 	Scanner useDelimiter(String str)	str 로 지정된 문자를 분리자로 설정
 	void close()						객체를 닫는다.
 
 */
	}

}
